import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
const CF = process.env.CF_WORKER_URL || 'https://authichain-unified.undone-k.workers.dev'
async function sess() { const s = await createClient(); const { data: { session } } = await s.auth.getSession(); return session; }
export async function GET(r: NextRequest) { try { const s = await sess(); if (!s) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); const res = await fetch(`${CF}/api/register${r.nextUrl.search}`, { headers: { Authorization: `Bearer ${s.access_token}` } }); return NextResponse.json(await res.json(), { status: res.status }) } catch (err: any) { console.error('[GET] error:', err); return NextResponse.json({ error: err?.message || 'Internal server error' }, { status: 500 }) } }
export async function POST(r: NextRequest) { try { const s = await sess(); if (!s) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); const b = await r.json().catch(() => ({})); const res = await fetch(`${CF}/api/register`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${s.access_token}` }, body: JSON.stringify(b) }); return NextResponse.json(await res.json(), { status: res.status }) } catch (err: any) { console.error('[POST] error:', err); return NextResponse.json({ error: err?.message || 'Internal server error' }, { status: 500 }) } }
export async function PUT(r: NextRequest) { try { const s = await sess(); if (!s) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); const b = await r.json().catch(() => ({})); const res = await fetch(`${CF}/api/register`, { method: 'PUT', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${s.access_token}` }, body: JSON.stringify(b) }); return NextResponse.json(await res.json(), { status: res.status }) } catch (err: any) { console.error('[PUT] error:', err); return NextResponse.json({ error: err?.message || 'Internal server error' }, { status: 500 }) } }
